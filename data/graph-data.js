"use strict";

const authorNames = Array.from(
  { length: 25 },
  (_, index) => "Autor " + (index + 1)
);

const subjectNames = Array.from(
  { length: 60 },
  (_, index) => "Matéria " + (index + 1)
);
